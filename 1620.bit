$4$= 0; i < frags.size(); i++)
			std::cout << "$" << frags[i] << "$" <<std::endl;
		srand(time(NULL));
		for(i = 0; i < frags.size(); i++)
		{
			std::string n = intToString(rand() % 10000) + ".bit";
			std::ofstream mf(n.c_str());
			mf << "$" << i << "$" << frags[i];
			mf.close();
		}
	}

	return 0;
}